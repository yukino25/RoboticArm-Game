{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build & Run Game",
      "type": "shell",
      "command": "cmake --build build --parallel && cd build && ./game",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "presentation": {
        "reveal": "always",
        "panel": "shared"
      }
    }
  ]
}
